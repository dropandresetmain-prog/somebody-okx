# <PROVIDER NAME> — Feasibility Evidence

> **CANDIDATE VALIDATION — NOT A SELECTION**
>
> This file is a reusable template. No provider data is asserted.
> Fill only after the demo gate (18 Sep 2026, 12:00 SGT) has produced an authoritative selection.

Status: UNFILLED TEMPLATE
Provider: _<name>_
Resource class: _<one of `proprietary_data` | `privileged_access` | `attestation` | `human_voice_contact` | `physical_presence` | `specialist_compute`>_
Access date: _<date evidence was gathered>_

## 1. Endpoint / API / tool

_What is the exact API endpoint, SDK method, or tool interface?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 2. Current availability

_Is the service currently live and accepting requests? Any waitlist, beta, or region restrictions?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 3. Current pricing

_Per-request, per-call, subscription, or tiered? Exact figures where public._

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 4. Required authentication

_API key, OAuth, wallet signature, other? What credentials are needed?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 5. Test / sandbox environment

_Is there a testnet, sandbox, or free-tier endpoint? What chain ID / environment?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 6. x402 / payment compatibility

_Can this provider be paid through the x402 protocol on X Layer? If not, what payment path?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 7. Network requirements

_Mainnet only? Testnet supported? Specific chain or network?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 8. Latency

_Expected response time for a single call? Compatible with 2–4 minute demo window?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 9. Rate limits

_Calls per second / per minute / per day?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 10. Result schema

_What does the response look like? Fields, types, structure._

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 11. Result verification / proof method

_How can Somebody independently verify the result? Signed envelope, read-back, on-chain receipt, other?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 12. Failure modes

_What happens on timeout, rate limit, partial failure, malformed input? Error schema?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 13. Duplicate-request / idempotency behavior

_Is it safe to retry? Does the provider return the same result for the same request? Idempotency keys?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 14. Terms / licensing restrictions relevant to the demo

_Data usage restrictions, redistribution limits, commercial-use constraints, attribution requirements?_

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 15. Scarcity justification

_Why is this resource genuinely externally controlled and not reproducible internally? Reference the Make-vs-Buy rule (`DECISIONS_LOG.md` 17 Sep)._

`EVIDENCED (source + access date)` or `UNVERIFIED — could not reach authoritative source (what was tried)`

## 16. Risks flagged by pre-build scan

_Carry forward any risks already identified in `docs/research/PREBUILD_RESEARCH_INGEST.md` for this provider/lead._

| Risk | Source |
|---|---|
| _<risk description>_ | _<file:line>_ |
